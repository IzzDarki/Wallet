<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WAKE_LOCK"
        android:maxSdkVersion="25" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name="com.izzdarki.wallet.Application"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:allowBackup="false">

        <!-- main activities -->
        <activity
            android:name="com.izzdarki.wallet.ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.izzdarki.wallet.ui.authentication.AuthenticationActivity"
            android:exported="true"
            android:windowSoftInputMode="stateVisible"
            android:noHistory="true"
            android:excludeFromRecents="true" /> <!-- noHistory and excludeFromRecents means the user cannot return to this activity using the back button -->
        <activity
            android:name="com.izzdarki.wallet.ui.authentication.AutofillAuthenticationActivity"
            android:exported="true"
            android:windowSoftInputMode="stateVisible"
            android:noHistory="true"
            android:excludeFromRecents="true" /> <!-- noHistory and excludeFromRecents means the user cannot return to this activity using the back button -->
        <activity
            android:name="com.izzdarki.wallet.ui.credentials.ShowCredentialActivity"
            android:configChanges="orientation"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.izzdarki.wallet.ui.credentials.EditCredentialActivity"
            android:configChanges="orientation"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.izzdarki.wallet.ui.credentials.ShowCredentialActivity" />
        </activity>

        <!-- other activities -->
        <activity android:name="com.izzdarki.wallet.ui.secondary.GetContentImageActivity" />
        <activity android:name="com.izzdarki.wallet.ui.secondary.ImageCaptureActivity" />
        <activity android:name="com.izzdarki.wallet.ui.secondary.CodeScannerActivity" />
        <activity android:name="com.izzdarki.wallet.ui.secondary.CropActivity" />

        <!-- services -->
        <service
            android:name="com.izzdarki.wallet.services.WalletAutofillService"
            android:label="@string/wallet_autofill_service"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service_configuration" />
        </service>

        <service
            android:name="com.izzdarki.wallet.services.ClearDirectoryService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <service
            android:name="com.izzdarki.wallet.services.CreateExampleCredentialService"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="@string/fileprovider_authority"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>